{
  "name": "medapp-api",
  "version": "1.0.0",
  "description": "API basica de medicamentos para MedApp",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
